class Solution(object):
    def hammingWeight(self, n):
        """
        :type n: int
        :rtype: int
        """
        count = 0
        j = n
        while j > 0:
            count += j % 2
            j = j // 2
        
        return count
